# HiveBuzz Environment Configuration

# Flask
FLASK_APP=app.py
FLASK_ENV=development
SECRET_KEY=your-secret-key-change-in-production
DEBUG=True
PORT=5000

# Hive API Endpoints
API_URL=https://api.hive.blog
DBUZZ_API_URL=https://api.dbuzz.com

# HiveSigner OAuth - UPDATED CONFIGURATION
# You must register your app at https://hivesigner.com/dashboard and use the exact same values
HIVESIGNER_APP_NAME=hivebuzz
# For development without client secrets (public clients):
HIVESIGNER_CLIENT_SECRET=
# IMPORTANT: This must exactly match the callback URI you registered at HiveSigner
HIVESIGNER_REDIRECT_URI=http://localhost:5000/hivesigner/callback
# Base HiveSigner URL (should not need changing)
HIVESIGNER_APP_HOST=https://hivesigner.com
# App URL for icon/branding
APP_URL=http://localhost:5000
# Only request minimal permissions for login
HIVESIGNER_SCOPE=login

# Database configuration (if needed)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=hivebuzz
# DB_USER=hivebuzz
# DB_PASSWORD=your-db-password
# DATABASE_PATH=custom/path/to/database.db  # Uncomment to override default
